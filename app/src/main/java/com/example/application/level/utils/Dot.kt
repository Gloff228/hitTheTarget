package com.example.application.level.utils

class Dot(x: Number, y: Number) {
    var x = x.toDouble()
    var y = y.toDouble()

    fun setPos(x: Number, y: Number) {
        this.x = x.toDouble()
        this.y = y.toDouble()
    }
}